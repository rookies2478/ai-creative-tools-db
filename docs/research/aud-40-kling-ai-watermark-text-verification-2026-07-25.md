# AUD-40 Kling AI 透かし文字列表記検証記録（HOLD）

- **確認日**: 2026-07-25
- **対象ツール**: Kling AI
- **対象項目**: 生成動画サンプルの透かし文字列表記
- **監査指摘**: `src/data/generatedVideos.ts`(50)および`src/pages/comparisons/ai-video-generation-sample-comparison/index.astro`(8)で、ツール名は「Kling AI」（スペースあり）に統一済みだが、透かし文字列としては「KlingAI 3.0」という詰め表記が使われている。製品内バージョン表記そのものの可能性があり断定不可、実際の透かし文言を現物確認の上で要否判断が必要とされていた。

## DB値
`src/content/tools/kling-ai.md`の`name: "Kling AI"`（スペースあり、確定）。

## 表示値
- `src/data/generatedVideos.ts:50` usageNote: "...透かし（KlingAI 3.0）が含まれています..."
- `src/pages/comparisons/ai-video-generation-sample-comparison/index.astro:8`: impression文中に「...透かし（KlingAI 3.0）が入っています。」

また、同じサンプル動画エントリの`model`フィールドは`"Kling AI 2.0"`（スペースあり・バージョン2.0）となっており、透かし文字列の「KlingAI 3.0」（詰め表記・バージョン3.0）とはスペースの有無だけでなくバージョン番号自体も食い違っている。

## 不一致分類
表示記号不統一の可能性、または製品内実際の透かし表示（現物）をそのまま書き起こしたものである可能性の両方が考えられ、断定できない。

## 一次情報
実際の生成動画ファイル（`/videos/generated/tools/kling-ai-tool-video-output-01.mp4`）を目視で確認する必要があるが、動画のフレーム内容（透かしの実際のピクセル表示）はテキストベースのツールでは確認できず、今回のバッチでは一次情報確認ができなかった。

## 採用した値
変更しない。

## 修正内容
なし。

## 修正しなかった内容
`generatedVideos.ts`のusageNoteおよび比較記事の該当箇所は、実際の動画に表示されている透かし文字列を管理者が目視で書き起こしたものである可能性があり、もしそうであれば「KlingAI 3.0」という詰め表記・バージョン番号は製品側の実際の表示そのものであり、これを「Kling AI」表記統一のために書き換えると、かえって実際の動画内容と乖離した誤情報になるリスクがある。

## 判断できなかった項目
- 実際の動画ファイルに表示されている透かし文字列が本当に「KlingAI 3.0」なのか、それとも記録時の誤記（本来は「Kling AI 2.0」だが手違いでバージョン・スペースが変わった）なのかを、動画を目視確認せずに判断できなかった。
- `model: "Kling AI 2.0"`と透かし文字列「KlingAI 3.0」のバージョン番号の食い違い自体も、どちらが正しいか判断できなかった。

## HOLDまたはNO_CHANGE理由
**HOLD**。動画ファイルの実際のフレーム内容を目視確認する手段が今回のバッチでは確保できず（ブラウザでの動画再生・フレームキャプチャが必要）、一次情報（動画の実際の透かし表示）を確認せずに文字列を変更すると、事実と異なる記述に書き換えてしまうリスクがある。監査報告書自身も「断定不可」「実際の透かし文言を現物確認の上」としており、この判断基準に従いHOLDとした。

## 構造上の課題
生成サンプル動画のメタデータ（`model`のバージョン番号）と、動画内に実際に焼き込まれている透かし文字列（usageNote内の自由記述）が別々に手動記録されており、両者の整合性を機械的に検証する手段がない。将来的には、透かし文字列を`model`とは独立した専用フィールド（例: `watermarkText`）として構造化し、動画の実物確認時にその場で記録することを推奨する。

## 他AUDとの関係
特に関連する既存AUDはないが、AUD-19（Kling AI運営国/会社情報）と同じKling AIツールに関する情報である。
